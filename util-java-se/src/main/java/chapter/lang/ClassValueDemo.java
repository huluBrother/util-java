package chapter.lang;

/**
 * 将计算的值与（潜在地）每种类型相关联。
 * 例如，如果动态语言需要为在消息发送呼叫站点遇到的每个类构造消息分派表，
 * 则可以使用ClassValue来缓存为每个遇到的类快速发送消息所需的信息。
 *
 */
public class ClassValueDemo {
    //无公有类型构造方法
    /*
    protected abstract T	computeValue(类<?> type)
计算给定类的派生值为 ClassValue 。
T	get(类<?> type)
返回给定类的值。
void	remove(类<?> type)
删除给定类的关联值。
     */
    public static void main(String[] args) {

    }
}

