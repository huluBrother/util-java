package org.lang;

public interface ZHXCharSequence {
    /**
     * length()
     *      返回长度
     * charAt(int index)
     *      指定获取
     * subSequence(int start, int end)
     *      指定截取
     * toString()
     *      返回整体
     * chars()
     *      ？？？
     * codePoints()
     *      ？？？
     */
}
